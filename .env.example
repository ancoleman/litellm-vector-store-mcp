# LiteLLM Vector Store MCP Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# REQUIRED CONFIGURATION
# =============================================================================

# Your LiteLLM API key
LITELLM_API_KEY=sk-your-api-key-here

# Default vector store ID (used when no specific store is requested)
# This can be any of your vector store IDs
LITELLM_VECTOR_STORE_ID=1111111111111111

# =============================================================================
# OPTIONAL CONFIGURATION
# =============================================================================

# LiteLLM base URL
# Option 1: Direct connection (default)
LITELLM_BASE_URL=https://litellm.psolabs.com

# Option 2: Via gcloud proxy (for secure access or local development)
# If you're using gcloud IAP proxy, use host.docker.internal to access host's localhost:
# LITELLM_BASE_URL=http://host.docker.internal:5006
#
# To set up gcloud proxy:
#   gcloud compute start-iap-tunnel litellm-proxy 443 --local-host-port=localhost:5006
#
# IMPORTANT: Use host.docker.internal (not localhost) so Docker can access your host machine!
# localhost inside Docker = the container itself (won't work)
# host.docker.internal = your host machine (works!)
#
# Alternative for Linux (if host.docker.internal doesn't work):
#   Use --network host in Docker args, then you can use localhost:5006
#   See QUICKSTART.md for details

# Vertex AI configuration (if your vector stores use Vertex AI)
VERTEX_AI_PROJECT=ngfw-coe
VERTEX_AI_LOCATION=us-east4

# =============================================================================
# MULTI-VECTOR STORE SUPPORT
# =============================================================================
#
# The MCP server now supports searching multiple vector stores!
#
# Available stores are automatically discovered from your LiteLLM instance
# via the /vector_store/list API endpoint. No need to configure them here.
#
# Usage with Claude:
#   "What vector stores are available?"
#   → Lists all stores dynamically
#
#   "Search x-corpus for authentication code"
#   → Searches by friendly name (no ID needed)
#
#   "Search vector store 111111111111 for examples"
#   → Searches by direct ID
#
# The server automatically resolves friendly names to IDs at runtime.
#
# To see your available stores, use the litellm_list_vector_stores tool
# or run: curl -H "Authorization: Bearer $LITELLM_API_KEY" \
#             https://litellm.psolabs.com/vector_store/list
#
# =============================================================================
